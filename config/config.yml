tlsConfig:
  certFile: "${TLS_CERT_FILE}"
  keyFile: "${TLS_KEY_FILE}"
database:
  host: "${DB_HOST}"
  name: "${DB_NAME}"
  password: "${DB_PASSWORD}"
  port: ${DB_PORT}
  user: "${DB_USER}"
  sslMode: "disable"
jwt:
  secret: "${JWT_SECRET}"